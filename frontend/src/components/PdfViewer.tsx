"use client";

import React from "react";

// Placeholder component retained for compatibility. The custom PDF viewer has been removed.
export default function PdfViewer({ src, filename }: { src: string; filename?: string }) {
  return (
    <div className="p-4 text-sm text-gray-500">
      Viewer removed. Open the file directly via the View link.
    </div>
  );
}
